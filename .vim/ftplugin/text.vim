" (.txt) FILETYPE SETTINGS
"==========================
    colorscheme obsidian
    setlocal ffs=dos,unix       " use windows line endings for new buffers
            
    " Set tab spacing to 2
    setlocal shiftwidth=2
    setlocal tabstop=2
    setlocal softtabstop=2
              
    " Disable numbers
    setlocal nonumber
    setlocal norelativenumber
