startup_message off
vbell off  # disable visual bell
defscrollback 5000  # default: 100
silencewait 15  # default: 30
shell "bash"
autodetach on  # not sure what this does yet


# Bottom Screens Line
# green/yellow:
#caption always "%{= kY}%?%-Lw%?%{= kG}%n*%f %t%?(%u)%?%{= kY}%?%+Lw%?"

# blue/white:
caption always "%{wk} %?%-Lw%?%{wb}[%n*%f %t]%?(%u)%?%{wk}%?%+Lw%? %{wk}"
